services:
  - type: web
    name: video-backend
    env: node
    region: singapore
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y python3 python3-pip ffmpeg
      pip3 install yt-dlp
      npm install
    startCommand: npm start
